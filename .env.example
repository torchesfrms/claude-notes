# 环境标识（本地开发为 development，生产环境为 production）
NODE_ENV=development

# 后端端口（可选）
PORT=3001

# 前端端口（可选）
VITE_PORT=3000

# API 基础地址（可选，默认使用相对路径通过代理）
VITE_API_BASE_URL=

# 管理员密码（生产环境必填）
ADMIN_PASSWORD=your_password_here

# 数据库连接（生产环境使用）
# DATABASE_URL=postgresql://user:password@host:port/database
